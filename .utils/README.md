# Data Utilities

Here you can find a `validate/` crate, for which the validation workflow uses to check the well-formedness of the data. 

There is also a `transform/` crate, that provides various utilites to transform data in bulk.